# Windows Compatibility Layer

Wine/Proton-derived, sandboxed. Scoped "best effort," not full
guaranteed compatibility, in v1
(`../../docs/architecture/05-app-ecosystem-compatibility.md`).

## Priority list process
Top-N most-requested Windows apps tracked and prioritized here (list
TBD — depends on Phase 3/4 alpha/beta user feedback, which doesn't exist
yet at scaffolding time). Mitigates the Risk Register item: "Windows app
compatibility layer can't cover all apps users need."

Skeleton only.
