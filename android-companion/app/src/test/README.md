# Unit tests (JUnit + MockK)

Planned first tests once `../main/kotlin/os/continuum/companion/pairing/`
exists:
- `PairingHandshakeTest` — mirrors
  `../../../../../phase-2-continuumd-pairing/tests/protocol/pairing_handshake_test.md`
  from the Android side.
- `DiscoveryStateTest` — BLE/mDNS discovery state transitions.
