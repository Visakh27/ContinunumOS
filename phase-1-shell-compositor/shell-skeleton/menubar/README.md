# Menu Bar

Global menu bar: system-wide app menu, clock, "Continuum Center" status
area (paired Android device(s), battery, signal, continuity feature
toggles — consumes `continuumd`'s D-Bus API once Phase 2 lands it).
Skeleton only.
