# Shared Scripts

Placeholder for cross-phase utility scripts (e.g. a future
`new-phase-scaffold.sh` to generate a new phase's README+tests/ skeleton
consistently, or `check-session-log.sh` to lint `SESSION_LOG.md`
entries). None written yet — added as real repeated needs emerge rather
than speculatively.
