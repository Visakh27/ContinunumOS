# CI Templates

Placeholder for CI pipeline fragments shared across phases (e.g. a
common "checkout + setup-rust" step) once a concrete CI system is
selected (see `../../phase-0-foundation/ci/README.md`).
