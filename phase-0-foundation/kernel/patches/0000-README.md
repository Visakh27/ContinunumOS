# Kernel patch series

Empty by design at this stage. First patches expected once a concrete
Tier-1 reference device is selected and its driver gaps against mainline
LTS are identified. Each patch must follow the process in
`../README.md` and reference an upstreaming tracking issue.
